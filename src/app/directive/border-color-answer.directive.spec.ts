import { BorderColorAnswerDirective } from './border-color-answer.directive';

describe('BorderColorAnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderColorAnswerDirective();
    expect(directive).toBeTruthy();
  });
});
